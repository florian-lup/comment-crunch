/**
 * Application-wide constants
 */

// API related constants
export const API_ENDPOINTS = {
  YOUTUBE: '/api/youtube',
  GEMINI: '/api/gemini',
};

// UI related constants
export const UI_CONSTANTS = {
  APP_NAME: 'Comment Crunch',
  APP_DESCRIPTION: 'Extract insights from YouTube comments',
  DEFAULT_TAGS: ['AI-Powered', 'YouTube Analysis', 'Fast Insights'],
};

// Feature flags
export const FEATURES = {
  ENABLE_GEMINI: true,
}; 